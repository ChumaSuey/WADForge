# Deepseek session resume

1. Check the script compiles and runs cleanly: `py_compile.compile('GUI.py', doraise=True)` + `import backend`
2. If everything is ok, update the documentation (README or any docs) reflecting recent changes
3. Stage + commit with a descriptive message, then push to GitHub
